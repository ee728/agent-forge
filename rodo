1. agent/llm.py — LLM 封装层
- 请求重试缺失 — API 调用没有 retry，一次 5xx/网络抖动手工 GG。utils/retry.py 是空壳没接进来
- raw_message 存储了完整 message 但缺失 role — resp_data_process 里 raw_message=resp_message，OpenAI 的 choice\[0\]message 本身不带 role 字段（API 返回里有 role），但 resp_message 是 dict 包含 role。实际上没问题，不过留个意
- ToolCall.arguments 类型标注不准 — 声明 dict[str, object] 但 JSON 解析出来实际是 dict[str, Any]，不会导致 bug 但不够精确
- finish_reason="error" 时依然返回了 content 为错误字符串 — 这个 content 会被追加到 self.messages，污染对话历史（agent 里 line 232 self.messages.append(resp.raw_message)）
2. agent/core.py — Agent 编排器
- _load_prompt 没有先清旧 system prompt — insert 了一条新的 system message 到 message\[0\]，但旧的 system prompt 仍然在列表里（除非之前没发过消息）。如果多次 /prompt，messages 里会积累多条 system 消息
- run() line 280 退出时一定会 _save_session — 即使用户按 Ctrl+C 或输入 /exit 后不想保存也没选择。另外 Ctrl+C 在 _get_user_input 捕获了直接 sys.exit(0)，跳过 /exit 命令逻辑，不会 _save_session，行为不一致
- __context_engineering 修改已有 message 的 content — 直接在 last["content"] 前拼接 reminder，改变了已有消息对象。如果后续 compress 或 reload，这条被篡改过的消息会保持污染状态
- _inject_skill 直接原地 msg["content"] += — 在 system prompt 末尾追加 skill 内容，不可逆。多次 load skill 会无限膨胀 system prompt，没有卸载/重置机制
- _is_todo_update 只认 action == "update" — 但 create 也应该重置计时器（说明 agent 正在规划任务）
- _compressed_summary 只用于 /save，从未实际压缩运行时上下文 — 即使调用了 compress_context，self.messages 仍然是完整历史，只在保存时精简。LLM 的上下文窗口并不会因此释放，这是命名/语义不符
- _list_history 返回所有文件，没有过滤 .json — 如果 history 目录有其他文件会一并列出
- _load_session 返回 bool 但 run() line 312 用了结果 — 正确。但加载 session 后 self._first_user_input 没有从 session 恢复，会导致后续 /save 覆盖文件名（容易覆盖旧 session）
- _clean_surrogates 每次 _process 入口调用一次 — O(n) 遍历整个 messages 列表，在长对话中重复调用有性能浪费。且应该只在 append 新消息之前调用即可
3. tools/base.py — 工具基础框架
- BaseTool.exe_result 是类变量（class var） — exe_result = ToolExecutionResult 在类级别定义。所有实例共享同一个类属性引用。虽然每个 execute 都会覆盖 self.exe_result，但并发场景有问题；更微妙的是 ToolExecutionResult 是 dataclass，类级别的那个值是类本身而非实例
- ToolRegistry.execute 里有个 print(exe_result) line 69 — 多余的 print，工具结果已经在 agent 层通过 __exe_result_info_show 打印了，这里会重复输出
- result_to_str 无法处理非 JSON-serializable 的 result — 如果 result 含有不可序列化对象会抛出异常且未被捕获
4. tools/shell.py — 命令执行
- shell=True 的安全风险 — 没有做任何命令校验/白名单，ls; rm -rf / 也会执行
- stderr 在 returncode=0 时被吞掉了 — output_str = (result.stdout or result.stderr).strip()，当 returncode=0 时有 stderr 也返回了，returncode!=0 时 stdout 被丢弃。更合理的应该是：成功时返回 stdout（stderr 可附加警告），失败时返回 stderr
- subprocess.run 没有 timeout 时的 fallback — arguments.get("timeout", 60) 参数是 int 传给 timeout，但如果 LLM 传入 "timeout": "30"（字符串），subprocess 会抛 TypeError
5. tools/edit_file.py — 文件编辑
- _write 不检查父目录是否存在 — os.makedirs(os.path.dirname(...) or ".", exist_ok=True)，如果 file_path 是 "relative/path.txt" 这里没问题，如果是纯文件名如 "file.txt"，os.path.dirname 返回 ""，or "." 兜底。但相对路径基于哪个 CWD 不确定，依赖 agent 的工作目录
- _edit 没有 backup — 替换是原地直接覆盖，如果替换过程中 crash 或 LLM 误操作，原始内容丢失且无法恢复
- MAX_CONTENT_CHARS = 2000 — 值较小，对大文件需要多次调用，但 description 中描述了这种预期，算设计选择不算 bug
- action 校验分散在两个地方 — parameters 的 enum 中已经约束了 ["read", "write", "edit"]，LLM 正常情况下不会传其他值，但 execute 里 else: _error 做了兜底。不算 bug，但参数描述的冗余度较高
6. tools/todo_task.py — 任务管理
- 类变量 _tasks / _next_id 是跨实例共享的 — 如果未来有多个 Agent 实例/多 session，todo 列表会乱串。当前是单例模式，暂时不是问题但设计上有隐患
- _list(filter_status) 参数名和 schema 里的参数名不一致 — schema 里是 status（不带 filter_ 前缀），execute line 135 调用 self._list(arguments.get("status")) 是对的。但 _list 的形参名容易让人困惑
- _clear 只保留 pending 状态的 — completed 和 failed 会被清掉，但 in_progress 也会被清掉（因为它不在保留条件里）。可能不是预期行为
- _clear 没有重置 _next_id — 清空任务后 ID 仍从断点继续，不会从 t-001 重新开始（不算 bug，看需求可算可不算）
- __render 使用 _renderer.log(..., to_shell=False) 拼接字符串 — 但 _renderer 是个 Logger 实例，这实际上是挺 clever 的 pattern（复用了颜色格式化），不过 Logger 本身就支持返回字符串，属于合理复用
7. tools/skill_loader.py — 技能加载
- _parse_front_matter 很脆弱 — 用简单的 content.startswith("---") + split 来解析 front matter，如果文档内容中出现 --- 会错误分割。应该处理真正的 YAML front matter（parts[1] 只取第一个 --- 和第二个 --- 之间）
- 多次 load 同一 skill 没有去重 — 重复 load 会多次追加到 system prompt（agent 层面没有检查），系统 prompt 会越来越臃肿
- list_skills 读文件开销 — 每次调用都遍历所有 skill 文件并解析 front matter。对少量 skill 不是问题，但有缓存优化的空间
8. tools/compress_context.py
- 仅存储 summary，不做任何实际压缩 — 工具只是让 LLM 生成一个 summary 字符串，然后存下来。核心的"清空旧消息释放上下文"逻辑完全没有实现。名为"压缩上下文"，实为"生成摘要"
- result_to_str 对 summary 的二次 JSON 化 — summary 已经是字符串了，ToolExecutionResult 再 JSON 序列化一次，LLM 读到的结果是 {"result": "some summary"} 而不是纯文本，增加了不必要的解析负担
9. main.py — 入口
- system_prompt 文件句柄没有 close — open("prompts/system.md").read() 不推荐，应该用 with open
- skill_tool.list_skills() line 38 返回的字符串直接拼接 — 没有 try/except，如果 skills 目录不存在或文件读取异常会直接崩溃
10. utils/logger.py — 日志
- 没有文件日志 — docstring 说"支持同时输出到控制台和文件"，实际只有控制台输出
- import logging 从未使用 — 导入了标准库 logging 但完全没有用
- log() 的 to_shell=False 返回值格式化没有问题，但每一行调用 log() 都会计算时间戳（即使 show_time=False 也有一次 datetime.now() 调用，不过开销很小）
11. prompts/system.md — 系统提示词
- "One Action Per Turn" 与 ReAct 循环不匹配 — 系统 prompt 说 "Return either a tool call OR a final response, never both"，但实际上 LLM API 的标准行为就是如此（一个 response 要么有 tool_calls，要么有 content）。这个规则是 LLM 的天然约束，写进 prompt 意义不大，除非你的 LLM 支持同时返回两者
- 没有说明会话压缩/Session 机制的存在 — 系统 prompt 没有告诉 LLM 有关 /save、/load、compress_context 的用法，LLM 不知道什么是 session 压缩
---
概览：5 个 Bug / 潜在错误，6 个功能缺失，若干设计瑕疵。建议优先修 base.py 的类变量问题、shell.py 的安全和 timeout 容错、以及 edit_file.py 的数据安全（backup）。后续按优先级逐一讨论优化方案。