# AGENTS.md

This repository is empty — a fresh starting point with no code, config, or dependencies yet.

- No build system, test framework, linter, or runtime configuration exists.
- No package manifests, lockfiles, or `opencode.json` configuration.
- There are no existing instruction files (`.cursorrules`, `CLAUDE.md`, etc.).

When adding new content, place instructions in this file as they become relevant. For now there are no repo-specific conventions to learn.
